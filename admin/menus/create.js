(() => {
  if(!document.querySelector('link[href="/admin/menus/image-upload.css"]')){const style=document.createElement('link');style.rel='stylesheet';style.href='/admin/menus/image-upload.css';document.head.append(style)}
  const form=document.querySelector('#menuForm'),category=document.querySelector('#categorySelect'),error=document.querySelector('#formError'),preview=document.querySelector('#imagePreview'),fileInput=document.querySelector('#imageFile'),removeButton=document.querySelector('#removeImage');const showError=message=>{error.textContent=message;error.hidden=!message};const setImage=value=>{form.elements.image.value=value||'';preview.hidden=!value;removeButton.hidden=!value;if(value)preview.src=value};category.innerHTML=CATEGORIES.map(item=>`<option value="${item.id}">${escapeHtml(item.name)}</option>`).join('');
  fileInput.addEventListener('change',()=>{const file=fileInput.files[0];if(!file)return;if(!['image/jpeg','image/png','image/webp'].includes(file.type)||file.size>2*1024*1024){showError('JPG, PNG, WEBP 형식의 2MB 이하 이미지를 선택해 주세요.');fileInput.value='';return}const reader=new FileReader();reader.onload=()=>{setImage(reader.result);showError('')};reader.readAsDataURL(file)});removeButton.addEventListener('click',()=>{fileInput.value='';setImage('')});
  form.addEventListener('submit',event=>{event.preventDefault();MomoAdmin.requireAdmin();const data=new FormData(form),menu={name:String(data.get('name')||'').trim(),category:String(data.get('category')||''),price:Number(data.get('price')),description:String(data.get('description')||'').trim(),image:String(data.get('image')||'')};const message=!menu.name?'메뉴명을 입력해 주세요.':!menu.category?'카테고리를 선택해 주세요.':!Number.isFinite(menu.price)||menu.price<0?'가격을 올바르게 입력해 주세요.':!menu.description?'설명을 입력해 주세요.':'';if(message){showError(message);return}const created=createMenu(menu);MomoAdminActivity?.log(MomoAdmin.user,'CREATE_MENU','MENU',created.id,'메뉴 등록');location.href=`/admin/menus/detail.html#id=${encodeURIComponent(created.id)}`});
})();
