(() => {if(!window.MomoAdmin)return;const admin=MomoAdmin.user,id=new URLSearchParams(location.search).get('id'),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),money=v=>`${Math.max(0,Number(v)||0).toLocaleString('ko-KR')}원`;let busy=false;const toast=m=>{adminToast.textContent=m;adminToast.hidden=false;setTimeout(()=>adminToast.hidden=true,2500)};const render=()=>{const o=getOrderById(id);if(!o){orderDetail.innerHTML='<div class="detail-card">주문을 찾을 수 없습니다.</div>';return}const progress=getOrderStatusProgress(o.status),next=getNextOrderStatus(o.status),items=o.items||[];orderDetail.innerHTML=`<section class="detail-head"><div><p class="admin-eyebrow">ORDER DETAIL</p><h1>${esc(o.orderNumber||o.id)}</h1><span class="status-pill status-${o.status}">${getOrderStatusLabel(o.status)}</span></div><div><small>주문일시</small><strong>${new Date(o.createdAt).toLocaleString('ko-KR')}</strong></div></section><section class="order-progress">${ADMIN_ORDER_STATUSES.map((s,i)=>`<div class="progress-step ${i<progress?'done':i===progress?'current':''}"><i>${i<progress?'✓':i+1}</i><span>${getOrderStatusLabel(s)}</span></div>`).join('')}</section><div class="detail-grid"><section class="detail-card"><h2>주문 및 고객 정보</h2><dl class="info-list"><div><dt>고객명</dt><dd>${esc(o.customerName||'-')}</dd></div><div><dt>이메일</dt><dd>${esc(o.userEmail||'-')}</dd></div><div><dt>전화번호</dt><dd>${esc(o.customerPhone||'-')}</dd></div><div><dt>픽업 매장</dt><dd>${esc(o.pickupStore?.name||'-')}</dd></div><div><dt>결제 수단</dt><dd>${esc(o.paymentMethod||'-')}</dd></div><div><dt>최종 수정</dt><dd>${o.updatedAt?new Date(o.updatedAt).toLocaleString('ko-KR'):'-'}</dd></div></dl><h2>주문 메뉴</h2><table class="detail-items"><tbody>${items.map(i=>`<tr><td><strong>${esc(i.name)}</strong><small>${esc(Object.values(i.options||{}).filter(Boolean).join(' · ')||'기본 옵션')}</small></td><td>${Number(i.quantity)||0}개</td><td>${money((Number(i.price)||0)*(Number(i.quantity)||0))}</td></tr>`).join('')}</tbody></table><div class="totals"><div><span>상품 금액</span><b>${money(o.subtotal)}</b></div><div><span>쿠폰 할인</span><b>-${money(o.couponDiscount)}</b></div><div><span>포인트 사용</span><b>-${money(o.pointDiscount)}</b></div><div class="final"><span>최종 결제 금액</span><b>${money(o.totalAmount??o.total)}</b></div></div></section><aside><section class="detail-card memo-box"><h2>관리자 메모</h2><textarea id="adminMemo" maxlength="1000" placeholder="고객 화면에는 표시되지 않습니다.">${esc(o.adminMemo)}</textarea><button id="saveMemo" type="button">메모 저장</button></section><section class="status-action"><p>현재 상태 <strong>${getOrderStatusLabel(o.status)}</strong></p><button id="advanceStatus" type="button" ${!next?'disabled':''}>${getNextOrderActionLabel(o.status)}</button></section></aside></div>`;saveMemo.addEventListener('click',()=>{try{saveOrderAdminMemo(o.id,admin,adminMemo.value);toast('관리자 메모가 저장되었습니다.')}catch(err){alert(err.message)}});advanceStatus.addEventListener('click',()=>{if(!next||busy)return;if(!confirm(`이 주문을 ${getOrderStatusLabel(next)} 상태로 변경할까요?`))return;busy=true;try{advanceOrderStatus(o.id,admin);toast(`주문 상태가 ${getOrderStatusLabel(next)}으로 변경되었습니다.`);render()}catch(err){alert(err.message)}finally{busy=false}})};render()})();
