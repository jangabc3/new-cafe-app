(() => {if(!window.MomoAdmin)return;const admin=MomoAdmin.user,id=new URLSearchParams(location.search).get('id'),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),date=v=>v?new Date(v).toLocaleString('ko-KR'):'-',toast=m=>{qnaToast.textContent=m;qnaToast.hidden=false;setTimeout(()=>qnaToast.hidden=true,2200)};const render=()=>{const q=MomoQna.getInquiryById(id);if(!q){detailArea.innerHTML='<section class="qna-detail-card">문의를 찾을 수 없습니다.</section>';return}const history=[...(q.answerHistory||[]),...(q.answer?[q.answer]:[])];detailArea.innerHTML=`<section class="qna-detail-head"><div><p class="admin-eyebrow">INQUIRY DETAIL</p><h1>${esc(q.title)}</h1><div class="qna-meta"><span>${esc(q.id)}</span><span>${MomoQna.getInquiryCategoryLabel(q.category)}</span><span>${esc(q.userName)} · ${esc(q.userEmail)}</span><time>${date(q.createdAt)}</time></div></div><span>${MomoQna.getInquiryStatusLabel(q.status)}</span></section><div class="qna-detail-grid"><div><section class="qna-detail-card"><h2>문의 내용</h2>${q.orderNumber?`<p>주문번호 <strong>${esc(q.orderNumber)}</strong></p>`:''}<div class="inquiry-content">${esc(q.content)}</div><h3>첨부파일</h3><div class="attachment-ready">첨부파일 기능 준비 중</div></section><section class="qna-detail-card" id="answer"><h2>관리자 답변</h2><form class="answer-form" id="answerForm"><textarea name="content" minlength="10" maxlength="2000" required placeholder="고객에게 전달할 답변을 작성해주세요.">${q.answer?esc(q.answer.content):''}</textarea><p><span id="answerCount">${q.answer?.content.length||0}</span> / 2000 · 자동 저장 기준 시각 ${date(new Date())}</p><button type="submit">${q.answer?'답변 수정':'답변 등록'}</button>${q.answer?'<div class="answer-actions"><button class="delete" id="deleteAnswer" type="button">답변 삭제</button></div>':''}</form></section><section class="qna-detail-card"><h2>답변 이력</h2><div class="answer-history">${history.map((a,i)=>`<article class="answer-version"><strong>${i+1}차 답변 ${a.action?`· ${a.action}`:''}</strong><div>${esc(a.content)}</div><small>${esc(a.adminName)} · ${date(a.updatedAt||a.createdAt)}${a.archivedAt?` · 보관 ${date(a.archivedAt)}`:''}</small></article>`).join('')||'<p>답변 이력이 없습니다.</p>'}</div></section></div><aside><section class="qna-detail-card admin-memo"><h2>관리자 메모</h2><p>고객 화면에는 표시되지 않습니다.</p><textarea id="adminMemo" maxlength="1000">${esc(q.adminMemo)}</textarea><button id="saveMemo" type="button">메모 저장</button></section></aside></div>`;const form=answerForm,textarea=form.content;textarea.addEventListener('input',()=>answerCount.textContent=textarea.value.length);form.addEventListener('submit',e=>{e.preventDefault();if(!form.reportValidity())return;if(!confirm(q.answer?'답변을 수정할까요?':'답변을 등록할까요?'))return;try{q.answer?MomoQna.updateAnswer(id,admin,textarea.value):MomoQna.createAnswer(id,admin,textarea.value);toast(q.answer?'답변이 수정되었습니다.':'답변이 등록되었습니다.');render()}catch(err){toast(err.message)}});document.querySelector('#deleteAnswer')?.addEventListener('click',()=>{if(confirm('답변을 삭제하고 문의를 답변 대기로 변경할까요?')){MomoQna.deleteAnswer(id,admin);toast('답변이 삭제되었습니다.');render()}});saveMemo.addEventListener('click',()=>{MomoQna.saveAdminMemo(id,admin,adminMemo.value);toast('관리자 메모가 저장되었습니다.')})};render()})();
