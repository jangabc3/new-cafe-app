(() => {if(window.MomoAdminActivity)return;const KEY='momoAdminActivityLogs',read=()=>{try{const v=JSON.parse(localStorage.getItem(KEY)||'[]');return Array.isArray(v)?v:[]}catch{return[]}},clean=v=>{if(!v||typeof v!=='object')return v;const copy={...v};delete copy.password;delete copy.passwordConfirm;return copy};function log(admin,action,entityType,entityId,description,before=null,after=null){if(admin?.role!=='ADMIN')throw Error('관리자 권한이 필요합니다.');const list=read();list.unshift({id:`ACT-${Date.now().toString(36)}-${Math.random().toString(36).slice(2,7)}`,adminId:admin.id??admin.email,adminName:admin.name||'관리자',action,entityType,entityId:entityId??null,description,before:clean(before),after:clean(after),createdAt:new Date().toISOString()});localStorage.setItem(KEY,JSON.stringify(list));return list[0]}window.MomoAdminActivity={KEY,getLogs:read,log}})();
