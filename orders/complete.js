(() => {const key='momo_coffee_latest_order_v1';const requested=new URLSearchParams(location.search).get('id');const latest=localStorage.getItem(key);if(!requested||requested!==latest){location.replace('list.html');return}const order=getOrderById(requested);if(!order||order.paymentStatus!=='paid'){location.replace('list.html');return}const names={card:'신용·체크카드',kakao:'카카오페이',naver:'네이버페이',toss:'토스페이',free:'무료 주문'};document.querySelector('#orderMeta').innerHTML=`<div><dt>픽업 매장</dt><dd>${escapeHtml(order.pickupStore?.name||'-')}</dd></div><div><dt>주문 번호</dt><dd>${escapeHtml(order.orderNumber||order.id)}</dd></div><div><dt>예상 준비 시간</dt><dd>약 ${Number(order.pickupStore?.eta)||10}분</dd></div><div><dt>결제 수단</dt><dd>${names[order.paymentMethod]||order.paymentMethod}</dd></div><div><dt>최종 결제 금액</dt><dd>${formatPrice(order.totalAmount??order.total)}</dd></div><div><dt>주문 상태</dt><dd>${getStatusLabel(order.status)}</dd></div>`;document.querySelector('#orderedItems').innerHTML=order.items.map(i=>`<div class="menu-row"><span>${escapeHtml(i.name)} × ${i.quantity}</span><strong>${formatPrice(i.price*i.quantity)}</strong></div>`).join('');document.querySelector('#historyLink').href=`detail.html?id=${encodeURIComponent(order.id)}`;document.querySelector('#completeCard').hidden=false})();
