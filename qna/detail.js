(() => {const user=JSON.parse(localStorage.getItem('momoCurrentUser')||'null'),q=MomoQna,id=new URLSearchParams(location.search).get('id'),item=q.getInquiryById(id),esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),date=s=>s?new Date(s).toLocaleString('ko-KR'):'-';if(!q.canViewInquiry(item,user)){detailArea.innerHTML='<section class="panel error">문의에 접근할 수 없습니다. 목록으로 이동합니다.</section>';setTimeout(()=>location.replace('/qna/list.html'),1200);return}const answer=item.answer?`<section class="answer-card"><span class="badge answered">답변 완료</span><h2>관리자 답변</h2><div class="meta"><span>${esc(item.answer.adminName)}</span><time>${date(item.answer.createdAt)}</time>${item.answer.updatedAt?`<span>수정 ${date(item.answer.updatedAt)}</span>`:''}</div><div class="content">${esc(item.answer.content)}</div></section>`:`<section class="answer-card"><h2>답변을 준비하고 있습니다.</h2><p>확인 후 빠르게 안내해드릴게요.</p></section>`;detailArea.innerHTML=`<header class="qna-head"><p class="eyebrow">MOMO Q&amp;A</p><h1>${esc(item.title)}</h1></header><section class="detail-card"><span class="badge ${item.status==='ANSWERED'?'answered':''}">${q.getInquiryStatusLabel(item.status)}</span><div class="meta"><span>${esc(item.id)}</span><span>${q.getInquiryCategoryLabel(item.category)}</span><span>${esc(item.userName)}</span><time>${date(item.createdAt)}</time>${item.updatedAt?`<span>수정 ${date(item.updatedAt)}</span>`:''}</div><div class="content">${esc(item.content)}</div>${q.canEditInquiry(item,user)?`<div class="actions"><a class="btn" href="/qna/edit.html?id=${encodeURIComponent(item.id)}">문의 수정</a><button class="btn secondary" id="deleteButton">문의 삭제</button></div>`:''}</section>${answer}<div class="actions"><a class="btn secondary" href="/qna/list.html">목록</a></div>`;deleteButton?.addEventListener('click',()=>{if(confirm('작성한 문의를 삭제할까요?\n삭제한 문의는 복구할 수 없습니다.')){q.deleteInquiry(item.id,user.id??user.email);sessionStorage.setItem('momoQnaToast','문의가 삭제되었습니다.');location.replace('/qna/list.html')}})})();

(() => { const message=sessionStorage.getItem('momoQnaToast'); const toast=document.querySelector('#toast'); if(message&&toast){sessionStorage.removeItem('momoQnaToast');toast.textContent=message;toast.hidden=false;setTimeout(()=>{toast.hidden=true},3000)} })();
